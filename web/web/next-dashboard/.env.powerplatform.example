# Power Platform / Power Pages integration example variables
# Copy to .env.local (never commit secrets) and set real values.

# Azure AD multi-tenant or single-tenant context
POWERPLATFORM_TENANT_ID="00000000-0000-0000-0000-000000000000"
# Azure AD application (service principal) client id
POWERPLATFORM_CLIENT_ID="00000000-0000-0000-0000-000000000000"
# (Store secret only in GitHub Actions secrets / local .env, never commit)
POWERPLATFORM_CLIENT_SECRET="<secret>"

# Dataverse / environment URL (org URL, not portal URL)
POWERPLATFORM_ENV_URL="https://org12345abcd.crm.dynamics.com"
# Optional website ID for Power Pages site (GUID)
POWERPLATFORM_WEBSITE_ID="00000000-0000-0000-0000-000000000000"

# Deployment mode: EMBED_IFRAME | STATIC_WEBFILES | EXTERNAL_CDN
POWERPLATFORM_DEPLOY_MODE="EMBED_IFRAME"

# If using external hosting (e.g., GitHub Pages / Azure Static Web Apps) specify public base URL
POWERPLATFORM_PUBLIC_BASE_URL="https://cdn.example.com/dashboard"

# If uploading as web files (STATIC_WEBFILES)
POWERPLATFORM_WEBFILE_PREFIX="dashboard"
