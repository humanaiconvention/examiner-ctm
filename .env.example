# Optional access gating (development / preview only)
# Provide either a plain password (VITE_ACCESS_PASSWORD) or a SHA-256 hex digest (VITE_ACCESS_PASSWORD_HASH).
# If both set, plain match is attempted first, then hash compare.
# NEVER commit real secrets here; this is an example only.
VITE_ACCESS_PASSWORD=
VITE_ACCESS_PASSWORD_HASH=
# Environment example
# Copy to .env (NOT committed) and adjust values.

# Enable or disable the preview gate overlay (true|false)
VITE_ENABLE_PREVIEW_GATE=true

# Optional: override the preview access phrase (default: haslam)
VITE_PREVIEW_PASSWORD=haslam

# Application Insights (preferred: connection string)
# Replace with your real connection string OR instrumentation key.
# If both are set connection string wins.
VITE_APPINSIGHTS_CONNECTION_STRING=InstrumentationKey=3136dab6-1928-4e00-a020-0931ca72568a;IngestionEndpoint=https://dc.services.visualstudio.com/
# Legacy instrumentation key fallback (same GUID shown for illustration; normally only set one)
VITE_APPINSIGHTS_KEY=3136dab6-1928-4e00-a020-0931ca72568a
# Sampling percentage (0-100)
VITE_APPINSIGHTS_SAMPLE=50
# Disable web vitals collection (privacy / perf isolation)
VITE_DISABLE_VITALS=false

# Public-facing initiative tagline (non-sensitive)
TAGLINE="We will know — together"

# Owner Auth (single-user login Option 1)
# Email allowed to authenticate
OWNER_EMAIL=ben@humanaiconvention.com
# Random high-entropy secret (generate and place only in real .env, NOT committed)
SESSION_SIGNING_SECRET=replace_me_with_64_hex_chars
# Max age of persistent session cookie (seconds) e.g. 86400 = 1 day
SESSION_MAX_AGE_SECONDS=86400
# Time-to-live for generated one-time login tokens (seconds) e.g. 600 = 10 min
LOGIN_TOKEN_TTL_SECONDS=600

# Power Platform example (do NOT commit secrets)
# PP_ENV_URL=https://contoso.crm.dynamics.com
# PP_SOLUTION_NAME=CoreAutomation
# PP_MANAGED=false
# PP_CLIENT_ID=
# PP_CLIENT_SECRET=
# PP_TENANT_ID=
# PP_SOLUTION_PROJECT=powerplatform/solutions/CoreAutomation
# PP_PACK_OUT=powerplatform/packed/CoreAutomation-repacked.zip
# PP_SOLUTION_ZIP=powerplatform/exports/CoreAutomation-unmanaged-2025-10-05.zip
# PP_UNPACK_OUT=powerplatform/solutions/CoreAutomation
